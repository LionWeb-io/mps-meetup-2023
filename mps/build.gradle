plugins {
    id("com.specificlanguages.mps") version "1.5.0"
}

repositories {
    mavenLocal()
    maven { url = uri("https://artifacts.itemis.cloud/repository/maven-mps") }
    mavenCentral()
}

dependencies {
    generation("io.lionweb.lionweb-mps:lionweb-mps:0.1.1")
}