// Generated by the Freon Language Generator.
import { FreValidator, FreError, FreCompositeTyper } from "@freon4dsl/core";
import { PropertiesEveryConcept } from "../../language/gen";
import { PropertiesNonOptionalsChecker } from "./PropertiesNonOptionalsChecker";

import { PropertiesReferenceChecker } from "./PropertiesReferenceChecker";
import { PropertiesWalker, PropertiesWorker } from "../../utils/gen";
import { freonConfiguration } from "../../config/FreonConfiguration";

/**
 * Interface 'PropertiesCheckerInterface' represents any object that traverses the model tree and checks
 * its nodes, where any errors are deposited in 'errorList'.
 * Every checker that is used by the validator 'PropertiesValidator' should implement this interface.
 */
export interface PropertiesCheckerInterface extends PropertiesWorker {
    errorList: FreError[];
}

/**
 * Class PropertiesValidator implements the validator generated from, if present, the validator definition,
 * otherwise this class implements the default validator.
 * The implementation uses the visitor pattern to traverse the tree. Class PropertiesWalker implements
 * the actual checking of each node in the tree.
 */
export class PropertiesValidator implements FreValidator {
    /**
     * Returns the list of errors found in 'modelelement'.
     * This method uses the visitor pattern to traverse the tree with 'modelelement' as top node,
     * where classes PropertiesNonOptionalsChecker, PropertiesReferenceChecker,  implements the actual checking of each node in the tree.
     *
     * @param modelelement
     * @param includeChildren if true, the children of 'modelelement' are also checked.
     * The default for 'includeChildren' is true.
     */
    public validate(modelelement: PropertiesEveryConcept, includeChildren: boolean = true): FreError[] {
        // initialize the errorlist
        const errorlist: FreError[] = [];

        // create the walker over the model tree
        const myWalker = new PropertiesWalker();

        // create the checker on non-optional parts
        let myChecker = new PropertiesNonOptionalsChecker();
        myChecker.errorList = errorlist;
        // and add the checker to the walker
        myWalker.myWorkers.push(myChecker);

        // create the checker on references
        myChecker = new PropertiesReferenceChecker();
        myChecker.errorList = errorlist;
        // and add the checker to the walker
        myWalker.myWorkers.push(myChecker);

        // add any custom validations
        for (let checker of freonConfiguration.customValidations) {
            checker.errorList = errorlist;
            myWalker.myWorkers.push(checker);
        }

        // do the work
        myWalker.walk(modelelement, () => {
            return includeChildren;
        });

        // return any errors
        return errorlist;
    }
}
