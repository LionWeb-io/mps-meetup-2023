// Generated by the Freon Language Generator.
import {
    FreEditor,
    FreEnvironment,
    FreReader,
    FreCompositeTyper,
    FreValidator,
    FreStdlib,
    FreWriter,
    FreInterpreter,
    FreScoperComposite,
    FreLanguageEnvironment,
    FreProjectionHandler
} from "@freon4dsl/core";
import { SimpleformsActions, initializeEditorDef, initializeProjections } from "../../editor/gen";
import { initializeScoperDef } from "../../scoper/gen/SimpleformsScoperDef";
import { initializeTypers } from "../../typer/gen/SimpleformsTyperDef";
import { SimpleformsValidator } from "../../validator/gen/SimpleformsValidator";
import { SimpleformsStdlib } from "../../stdlib/gen/SimpleformsStdlib";
import { SimpleformsModelUnitWriter } from "../../writer/gen/SimpleformsModelUnitWriter";
import { SimpleformsModelUnitReader } from "../../reader/gen/SimpleformsModelUnitReader";
import { MainSimpleformsInterpreter } from "../../interpreter/MainSimpleformsInterpreter";
import { MainModel, Form, initializeLanguage } from "../../language/gen";

/**
 * Class SimpleformsEnvironment provides the link between all parts of the language environment.
 * It holds the currently used editor, scoper, typer, etc, thus providing an entry point for
 * for instance, the editor to find the right scoper, or for the validator to find the typer
 * to use.
 * This class uses the singleton pattern to ensure that only one instance of the class is present.
 */
export class SimpleformsEnvironment implements FreEnvironment {
    private static environment: FreEnvironment; // the only instance of this class

    /**
     * This method implements the singleton pattern
     */
    public static getInstance(): FreEnvironment {
        if (this.environment === undefined || this.environment === null) {
            this.environment = new SimpleformsEnvironment();
            FreLanguageEnvironment.setInstance(this.environment);
        }
        return this.environment;
    }

    /**
     * A private constructor, as demanded by the singleton pattern.
     */
    private constructor() {
        const actions = new SimpleformsActions();
        const myComposite = new FreProjectionHandler();
        this.editor = new FreEditor(myComposite, this, actions);
        initializeLanguage();
        initializeProjections(myComposite);
        initializeEditorDef();
        initializeScoperDef(this.scoper);
        initializeTypers(this.typer);
    }

    /**
     * Returns a new model with name 'modelName'.
     *
     * @param modelName
     */
    newModel(modelName: string): MainModel {
        const model = new MainModel();
        model.name = modelName;
        return model;
    }

    // the parts of the language environment
    editor: FreEditor;
    scoper: FreScoperComposite = new FreScoperComposite("main");
    typer: FreCompositeTyper = new FreCompositeTyper("main");
    stdlib: FreStdlib = SimpleformsStdlib.getInstance();
    validator: FreValidator = new SimpleformsValidator();
    writer: FreWriter = new SimpleformsModelUnitWriter();
    reader: FreReader = new SimpleformsModelUnitReader();
    interpreter: FreInterpreter = new MainSimpleformsInterpreter();
    languageName: string = "simpleforms";
    fileExtensions: Map<string, string> = new Map([["Form", "for"]]);
}
